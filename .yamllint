---
extends: default

rules:
  comments:
    # Dependabot writes SHA pins as "@<sha> # v1.2.3" with a single space.
    min-spaces-from-content: 1
  line-length:
    max: 120
    level: warning
  truthy:
    check-keys: false
